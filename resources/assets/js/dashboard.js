import Form from './core/Form';
import Modal from './components/Modal.vue';
import Multiselect from 'vue-multiselect'

if ($('body[view-name="home"]').length > 0) {
    window.vue = new Vue({
        el: '#app',
        components: {
        },
        data: {
        },
        mounted() {

        },
        updated(){
        },
        watch: {

        },
        methods: {
        },
    });
}